name: chefai
description: A multi-platform app to generate recipes based on ingredients and diet preferences.
version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0 # For making API requests

dev_dependencies:
  flutter_test:
    sdk: flutter