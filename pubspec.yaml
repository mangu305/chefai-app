name: recipe_generator_app
description: A multi-platform app to generate recipes based on ingredients and diet preferences.
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.15.0 # For making API requests

dev_dependencies:
  flutter_test:
    sdk: flutter